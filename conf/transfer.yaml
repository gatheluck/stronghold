defaults:
  - misc_transfer
  - online_logger: comet
  - dataset: cifar10
  - optimizer: sgd
  - scheduler: steplr

hydra:
  run:
    dir: ../logs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
  sweep:
    dir: ../logs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
    subdir: ${hydra.job.override_dirname}